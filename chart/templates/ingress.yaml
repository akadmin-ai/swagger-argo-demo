apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: current-time-service-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: demo.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: current-time-service
            port:
              number: 80
